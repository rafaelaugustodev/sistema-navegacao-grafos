# Shared

A pasta `shared` será utilizada para armazenar códigos compartilhados entre o frontend e o backend do projeto.

Aqui ficarão:
- algoritmos;
- estruturas de dados;
- tipos;
- lógica do grafo;
- implementações reutilizáveis.

O objetivo é evitar duplicação de código e manter a organização do sistema.